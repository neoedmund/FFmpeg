/*
tfg sampleConfig
*/
runnable main {
	wd = '../ffmpeg_b'
	execp ( '.' 'mkdir' wd )
	cmd = '../ffmpeg/configure'
	opts = parseData ( file ( 'sampleConfig.conf' ) ) . option

	log ( 'opts=%s' '' + opts )
	ret = execp ( wd cmd opts )
	log ( '%s' '' + ret )
}

